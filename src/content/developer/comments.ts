export const comments: string[] = [
  "/**\n * Fetches a user by their unique identifier.\n * @param id - The user's UUID\n * @returns A Promise resolving to the User object\n * @throws {NotFoundError} When no user matches the given id\n */",
  "def calculate_interest(principal: float, rate: float, years: int) -> float:\n    \"\"\"Calculate compound interest over a given period.\n\n    Args:\n        principal: Initial investment amount\n        rate: Annual interest rate (as decimal)\n        years: Number of compounding periods\n\n    Returns:\n        The total amount after interest\n    \"\"\"",
  "// TODO: Replace this naive implementation with a proper caching layer\n// FIXME: Race condition when multiple requests arrive simultaneously\nconst data = await fetchData(); // TODO: add error handling",
  "# NOTE: This migration must run before the new service is deployed\n# REVERT: abc1234 - This change caused a performance regression, reverting\n# HACK: Using eval here because the JSONP callback name is dynamic",
  "// This is a temporary workaround for a WebKit bug (webkit#184233)\n// The overflow hidden prevents a rendering glitch on iOS Safari\n.container {\n    overflow: hidden; /* workaround: remove when WebKit bug is fixed */\n}",
];
