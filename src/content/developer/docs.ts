export const docs: string[] = [
  "# Project Name\n\nA brief description of what this project does and who it's for.\n\n## Installation\n\n```bash\nnpm install my-package\n```",
  "## API Reference\n\n### `GET /api/users`\n\nReturns a paginated list of users.\n\n**Query Parameters:**\n- `page` (number): Page number, defaults to 1\n- `limit` (number): Items per page, defaults to 20",
  "## Migration Guide: v2 to v3\n\n### Breaking Changes\n\n1. The `onClick` handler now receives an event object instead of a string.\n2. Removed deprecated `componentWillMount` lifecycle method.",
  "## Changelog\n\n### [3.0.0] - 2024-06-15\n\n#### Added\n- Support for Server Components\n- New search API endpoint\n\n#### Fixed\n- Memory leak in subscription handling\n- Race condition in file upload",
  "## Troubleshooting\n\n### Error: \"Module not found\"\n\n1. Clear node_modules: `rm -rf node_modules`\n2. Clear package lock: `rm package-lock.json`\n3. Reinstall: `npm install`\n4. Restart the dev server",
  "## Architecture\n\nThe application follows a layered architecture:\n- **Presentation Layer**: React components\n- **Application Layer**: Services and use cases\n- **Domain Layer**: Business logic and entities\n- **Infrastructure Layer**: Database, external APIs",
  "## Getting Started\n\n1. Clone the repository\n2. Install dependencies: `npm install`\n3. Copy `.env.example` to `.env` and fill in values\n4. Run the dev server: `npm run dev`\n5. Open http://localhost:3000",
  "## Contributing\n\n1. Fork the repository\n2. Create a feature branch: `git checkout -b feat/my-feature`\n3. Commit your changes using conventional commits\n4. Push to your fork and submit a pull request\n5. Wait for review and CI checks to pass",
  "## Configuration\n\nThe application supports the following environment variables:\n- `DATABASE_URL`: PostgreSQL connection string\n- `REDIS_URL`: Redis connection string\n- `PORT`: Server port (default: 3000)\n- `LOG_LEVEL`: Logging level (debug, info, warn, error)",
  "### Rate Limiting\n\nAPI requests are limited to 100 requests per minute per IP address.\n\nHeaders included:\n- `X-RateLimit-Limit`: Maximum requests per window\n- `X-RateLimit-Remaining`: Requests remaining in window\n- `X-RateLimit-Reset`: Time when window resets (Unix timestamp)",
  "## Deployment\n\n### Prerequisites\n- Docker and Docker Compose installed\n- Node.js 20 or later\n- PostgreSQL 16\n\n### Steps\n1. Build the Docker image: `docker compose build`\n2. Run migrations: `docker compose run app npm run migrate`\n3. Start services: `docker compose up -d`",
  "## Testing\n\n```bash\n# Run unit tests\nnpm run test\n\n# Run integration tests\nnpm run test:integration\n\n# Run e2e tests\nnpm run test:e2e\n\n# Generate coverage report\nnpm run test:coverage\n```\n\nTests use Vitest and Playwright.",
  "### WebSocket Events\n\nThe server emits the following events:\n- `order.created`: When a new order is placed\n- `order.status_changed`: When order status updates\n- `payment.completed`: When payment succeeds\n- `payment.failed`: When payment fails\n\nSubscribe using the `ws` library.",
  "## FAQ\n\n**Q: How do I reset my password?**\nA: Use the `/api/auth/reset-password` endpoint with your email.\n\n**Q: Is there a free tier?**\nA: Yes, the free tier includes 1000 API calls per month.",
  "## Performance Benchmarks\n\n| Operation | Average | p95 | p99 |\n|-----------|---------|-----|-----|\n| User Lookup | 2ms | 5ms | 15ms |\n| Order Creation | 45ms | 120ms | 300ms |\n| Search Query | 80ms | 200ms | 500ms |\n\nBenchmarks run on a t3.medium instance.",
];
